import 'package:flutter/material.dart';

class TodosLosAdsScreen extends StatefulWidget {
  TodosLosAdsScreen();

  @override
  _TodosLosAdsScreenState createState() => _TodosLosAdsScreenState();
}

class _TodosLosAdsScreenState extends State<TodosLosAdsScreen> {
  _TodosLosAdsScreenState();

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Column(
        children: [
          SizedBox(height: 10),
        ],
      )
    );
  }
}
